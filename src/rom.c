//#include <stdio.h>
//#include <stdlib.h>
#include "rom.h"


void loadROM() {

/*
	FILE *file;
	if((file = fopen("rom/kernal", "r")) == NULL) {
		printf("cannot open kernal-rom file \"rom/kernal\"\n");
		exit(1);
	}
	fread(&kernalROM, 0x2000, 1, file);
	fclose(file);
	if((file = fopen("rom/basic", "r")) == NULL) {
		printf("cannot open basic-rom file \"rom/basic\"\n");
		exit(1);
	}
	fread(&basicROM, 0x2000, 1, file);
	fclose(file);
	if((file = fopen("rom/chargen", "r")) == NULL) {
		printf("cannot open character-rom file \"rom/chargen\"\n");
		exit(1);
	}
	fread(&characterROM, 0x1000, 1, file);
	fclose(file);
*/
}
